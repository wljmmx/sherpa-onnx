name: docker-image-linux-x64

on:
  push:
    branches:
      - master
    paths:
      - 'Dockerfile'
      - '.github/workflows/docker-image-linux-x64.yaml'
    tags:
      - 'v1.13.6'
  workflow_dispatch:

concurrency:
  group: docker-image-linux-x64
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push sherpa-onnx image (Linux x64)
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/sherpa-onnx:1.13.6
            ghcr.io/${{ github.repository_owner }}/sherpa-onnx:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max